# TODO: Create Selenium Automation for Facebook Icon Clickable Check

- [ ] Create a new Python file named 'facebook_icon_clickable_check.py' with Selenium code to:
  - Open facebook.com in full screen (maximized) mode.
  - Navigate to the login page (facebook.com is the login page).
  - Locate the Facebook icon (logo).
  - Check if the icon is clickable (visible, enabled, and can be clicked).
  - Print the result in the IDE (whether it's clickable or not).
- [ ] Ensure the script uses ChromeDriver with webdriver_manager for automatic driver management.
- [ ] Handle exceptions and close the browser properly.
